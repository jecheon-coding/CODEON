# 의존성
node_modules/

# 빌드 결과물
.next/
out/
build/
dist/

# 환경 변수 (비밀 정보 — 절대 커밋 금지)
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# TypeScript 빌드 캐시
tsconfig.tsbuildinfo
*.tsbuildinfo

# OS / IDE
.DS_Store
Thumbs.db
.vscode/settings.json

# Vercel
.vercel

# 학습 교재 원본 파일 (PDF, 마크다운 초안 — 저장소 비대화 방지)
book/
python/

# 로컬 메모/버그 보고서
*.txt
